server:
  port: 8080
  tomcat:
    uri-encoding: UTF-8
  servlet:
    context-path: /order
spring:
  mvc:
    view:
      prefix: /WEB-INF/jsp
      suffix: .jsp
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lianxi?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 112233..
mybatis:
  mapper-locations: classpath:com/acan/mapper/*.xml